# Local-only upstream failure simulation. Do not set these in production.
# Restart `npm run dev` after changing server/.env because dotenv reads it at process startup.
# MOCK_FAIL_WEIBO=1
# MOCK_FAIL_ALL=1
